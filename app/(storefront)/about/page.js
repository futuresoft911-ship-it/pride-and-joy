import AboutPageClient from './AboutPageClient';

export const metadata = {
  title: "About Us | Pride & Joy",
  description: "Learn more about Pride & Joy. We exist to celebrate every identity through premium, expressive fashion.",
};

export default function AboutPage() {
  return <AboutPageClient />;
}
